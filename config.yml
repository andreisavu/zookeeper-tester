
clusterName: asavu-zookeeper
clusterSize: 3

# AWS Credentials 
whirr:
  identity: identity
  credential: credential

logging:
  level: INFO
  console:
    enabled: true
    threshold: INFO
  loggers:
    net.schmizz.sshj: ERROR

